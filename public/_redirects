# Netlify Redirects - Order matters! More specific rules first.
# The "!" flag forces the redirect even if the file exists.

# Legacy URL redirect
/amazon.html  /products.html  301!

# Affiliate click tracking - MUST be above catch-all
/go  /.netlify/functions/go  200!
/go/*  /.netlify/functions/go  200!

# API function - MUST be forced to prevent file overrides
/api/*  /.netlify/functions/api  200!

# Admin routes - protect from SPA rewrite
/admin/*  /admin/:splat  200
/admin  /admin/  301

# SPA catch-all - MUST be last
/*  /index.html  200
